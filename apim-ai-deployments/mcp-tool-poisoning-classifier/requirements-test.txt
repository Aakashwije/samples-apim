# Dependencies needed to run the service's unit tests.
#
# The tests inject a stub classifier and a stub tokenizer, so torch, setfit and
# transformers are deliberately absent: the suite runs in seconds and needs no
# model download.

fastapi>=0.115,<1.0
pydantic>=2.7,<3.0
anyio>=4.0,<5.0
httpx>=0.27
pytest>=8.0
